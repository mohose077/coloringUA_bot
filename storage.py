def log_user_choice(user_id, choices):
    print(f"USER {user_id} вибрав: {choices}")

def log_user_feedback(user_id, image_url, feedback):
    print(f"USER {user_id} надав оцінку {feedback} для {image_url}")
